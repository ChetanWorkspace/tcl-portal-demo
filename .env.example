# Copy to .env.local and fill in values (never commit .env.local)

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_DESIGNS_BUCKET=product_image

# Server-only: product sync + Bubble migration script
SUPABASE_SERVICE_ROLE_KEY=

# Bonus 2 — Google AI (Gemini) for dashboard “what happens next”
GOOGLE_API_KEY=
